 * modify environment for execve
 * convert execl, execle and execlp to execv, execve and execvp respectively
 * sandbox
    * parse environment arguments
    * enforce sandbox on system calls
 * redirect - plugin for redirecting writes to another directory hierachy
